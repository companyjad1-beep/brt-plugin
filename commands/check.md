---
description: "AI 티 판정만 — 티 밀도 버킷 + 원문 인용 스팬 (brtwritenotai check 모드)"
---
`${CLAUDE_PLUGIN_ROOT}/skills/brtwritenotai/SKILL.md`를 읽고 **check 모드**로 실행하라 (수정 없이 판정만).

입력: $ARGUMENTS (판정할 텍스트 또는 파일 경로)
