---
description: "앱스토어/플레이스토어 출시 자산 자동 생성 — 마케팅 포인트 도출→소개글→화면 캡처→규격 스크린샷 렌더링 (store-shots 스킬)"
---
`${CLAUDE_PLUGIN_ROOT}/skills/store-shots/SKILL.md`를 읽고 그대로 실행하라. 렌더러와 설정 예시는 같은 폴더의 `generate.py`, `config.example.yaml`이다.

입력: $ARGUMENTS (대상 앱 프로젝트 경로 — 없으면 현재 디렉토리를 대상 앱으로 간주)
